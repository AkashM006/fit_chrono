20. Find out a neat way to chain validators
24. Implement debouncing when searching
37. Restrict reps to be within 2 digit range
38. After workout wave complete and after quitting the wave show some message in the snack bar
39. When app goes to background during workout, handle those cases
40. Account for skipping workout instead of completing it during logs
41. In the workout wave complete track properly the amount of worokuts completed
43. Get from the user after workout the intensity level
45. Maybe show progress even when showing rest workout but different like 3 done 3 more to go
46. When skipping a workout, immmediately after the skip is clicked pause the timer ( IN PROGRESS )
47. Display the workout log in the complete screen

LATER:
23. After implmementing custom code gen, try to change Implementation for edit workout wave, update only when required (MAYBE LATER)
28. Change muscle map to add search ( LATER )


OVERTHINKING:
1. Refactor all shared dialog widgeets (Use maybe only one base dialog widget and reuse them by providing parameters) (OVERTHINKING)
11. Change count in WorkoutWithMeasure to double from int (OVERTHINKING)
18. Add PopScope to StackWithLoaderWidget (Later: If required then for other screens)
21. Maybe implmement a watch<Entity> method for each get<Entity> ( OVERTHINKING )


DONE:
8. Implement search for searchable workout list ( DONE )
10. Run dart run custom_lint --fix to upgrade to the new riverpod and fix lint issues (View mail for more information) ( DONE )
13. Rename all update to edit and remove to delete ( DONE )
22. Refactor all <Entity>_form and <Entity>_detail screens to use StackWithLoaderWidget and <Dto>.init() ( DONE )
25. By default when installing the app, keep some muscle maps, workouts and one workout wave ( DONE )
26. When search is empty, show some text ( DONE )
27. When adding workout with some search keyword send the name also to add workout ( DONE )
29. All form after submit must not push to another page but just get reset ( DONE )
30. Make the muscle map select modalBottomSheet take the entire screen ( DONE )
31. Error occurs when inserting there are duplicates ( DONE )
32. Issue with muscle map and workouts edit ( DONE )
33. Make all _impl classes have const constructor ( DONE )
34. Are you sure you wanna delete for workout and workout wave ( DONE )
36. When adding rest to a workout wave only time must be enabled ( DONE )
42. Show at which workout the user is currently at ( DONE )
35. On workout wave edit enable the button when workout order is changed ( DONE )
44. Implement toJson and fromJson method for all models to enable storing them in db as string
    - Write toJson and fromJson methods for all models ( DONE )
    - Integrate with log feature ( DONE )