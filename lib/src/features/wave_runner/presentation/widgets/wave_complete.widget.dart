import 'package:flutter/material.dart';

class WaveCompleteWidget extends StatelessWidget {
  const WaveCompleteWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Completed"),
    );
  }
}
