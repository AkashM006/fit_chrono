import 'package:flutter/material.dart';

class WorkoutFormDialogWidget extends StatelessWidget {
  const WorkoutFormDialogWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
